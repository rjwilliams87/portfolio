import { Layout } from '../../components/Layout';

// const text = 'Home';

/**
 *
 * it should
 * display a list of desktop icons
 *
 * be responsive
 *
 * have a nav bar
 */

const Home = () => <Layout />;

export default Home;
